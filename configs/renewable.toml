# Scarce renewable Points for bounded World / RolloutCollector experiments.
# Finite-horizon training is a separate work item; use default.toml for training.
initial_points = 3
point_generation_probability_min = 0.1
point_generation_probability_max = 0.3
